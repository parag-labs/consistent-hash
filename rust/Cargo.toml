[package]
name = "consistent-hash"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Consistent hashing strategy family: ring, rendezvous, jump, Maglev, bounded-load"
repository = "https://github.com/parag-labs/consistent-hash"

[lib]
name = "consistent_hash"
path = "src/lib.rs"

[dev-dependencies]
serde_json = "1"
